# NETWORK TOPOLOGY & STRUCTURE - Networks
# Version: 1.0.0 | Domain: NETWORK SCIENCE (Holistic)
# Based on: Network Science (2025), Graph Theory (2025)
# PARADIGM SHIFT: From individual nodes/edges → Emergent whole-network structural properties

perspective: network_topology_structure
version: "1.0.0"

# ============================================================================
# CORE IDENTITY
# ============================================================================

network_name:
  # morph: text
  ""

network_type:
  # morph: badge
  # undirected | directed | weighted | bipartite | multiplex | temporal
  status: ""
  variant: ""

system:
  # morph: badge
  # biological | social | technological | information | infrastructure | ecological
  status: ""
  variant: ""

# ============================================================================
# BASIC PROPERTIES
# ============================================================================

nodes:
  # morph: number
  # N
  0

edges:
  # morph: number
  # E or L
  0

density:
  # morph: gauge
  # E / (N(N-1)/2) for undirected (%)
  current: 0
  target: 0
  unit: "%"

average_degree:
  # morph: number
  # <k>
  0

# ============================================================================
# DEGREE DISTRIBUTION - Connectivity pattern
# ============================================================================

degree_distribution:
  # morph: badge
  # poisson | power_law | exponential | bimodal | uniform
  status: ""
  variant: ""

distribution_parameters:
  # morph: text
  # e.g., "γ=2.5" for power law
  ""

scale_free:
  # morph: badge
  # yes | no
  # Power-law P(k) ~ k^(-γ)
  status: ""
  variant: ""

power_law_exponent:
  # morph: number
  # γ (gamma)
  0

maximum_degree:
  # morph: number
  # kmax
  0

degree_heterogeneity:
  # morph: badge
  # high | moderate | low
  status: ""
  variant: ""

# ============================================================================
# HUB STRUCTURE - Highly connected nodes
# ============================================================================

hubs_present:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

hub_count:
  # morph: number
  # Nodes with k > threshold
  0

top_hubs:
  # morph: list
  items:
    - node: ""
      degree: 0
      betweenness: 0
      function: ""

hub_distribution:
  # morph: badge
  # centralized | distributed
  status: ""
  variant: ""

# ============================================================================
# CLUSTERING - Local connectivity
# ============================================================================

global_clustering_coefficient:
  # morph: number
  # C (0-1)
  0

average_local_clustering:
  # morph: number
  # <C_i> (0-1)
  0

transitivity:
  # morph: number
  # Fraction of triangles
  0

triangles:
  # morph: number
  # Total triangles
  0

# ============================================================================
# PATH LENGTHS - Distances
# ============================================================================

average_path_length:
  # morph: number
  # <l>
  0

diameter:
  # morph: number
  # Maximum shortest path
  0

radius:
  # morph: number
  # Minimum eccentricity
  0

characteristic_path_length:
  # morph: number
  # Median shortest path
  0

# ============================================================================
# SMALL-WORLD PROPERTY
# ============================================================================

small_world:
  # morph: badge
  # yes | no
  # High clustering, short paths
  status: ""
  variant: ""

small_world_coefficient:
  # morph: number
  # σ = (C/Crand) / (L/Lrand)
  0

watts_strogatz_metric:
  # morph: number
  # S = C/L
  0

# ============================================================================
# CENTRALITY MEASURES - Node importance
# ============================================================================

degree_centrality:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

betweenness_centrality:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

closeness_centrality:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

eigenvector_centrality:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

pagerank:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

katz_centrality:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

top_central_nodes:
  # morph: list
  items:
    - node: ""
      centrality_measure: ""
      score: 0
      role: ""

# ============================================================================
# COMMUNITY STRUCTURE - Modularity
# ============================================================================

communities:
  # morph: number
  # Detected modules
  0

modularity:
  # morph: number
  # Q (0-1, typically 0.3-0.7 for real networks)
  0

community_detection_algorithm:
  # morph: badge
  # louvain | leiden | infomap | label_propagation | girvan_newman | spectral
  status: ""
  variant: ""

hierarchical_structure:
  # morph: badge
  # yes | no
  # Nested communities
  status: ""
  variant: ""

community_sizes:
  # morph: distribution
  categories:
    - size_range: ""
      count: 0
      percentage: 0

overlapping_communities:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

# ============================================================================
# ASSORTATIVITY - Mixing patterns
# ============================================================================

assortativity_coefficient:
  # morph: number
  # r (-1 to 1)
  0

assortativity_type:
  # morph: badge
  # assortative | disassortative | neutral
  # Assortative: hubs connect to hubs; Disassortative: hubs to periphery
  status: ""
  variant: ""

degree_correlation:
  # morph: badge
  # positive | negative | none
  status: ""
  variant: ""

# ============================================================================
# CORE-PERIPHERY STRUCTURE
# ============================================================================

core_periphery:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

core_size:
  # morph: gauge
  # Percentage in core (%)
  current: 0
  target: 0
  unit: "%"

core_nodes:
  # morph: list
  items:
    - node: ""
      coreness: 0

k_core:
  # morph: number
  # Maximum k-core
  0

# ============================================================================
# MOTIFS - Small subgraphs
# ============================================================================

network_motifs:
  # morph: badge
  # analyzed | not_analyzed
  status: ""
  variant: ""

motif_types:
  # morph: list
  items:
    - motif: ""
      count: 0
      enrichment: 0
      function: ""

feed_forward_loops:
  # morph: number
  # Common in regulatory networks
  0

bi_fan:
  # morph: number
  0

# ============================================================================
# CONNECTED COMPONENTS
# ============================================================================

connected:
  # morph: badge
  # yes | no
  # Single component
  status: ""
  variant: ""

components:
  # morph: number
  # Number of connected components
  0

giant_component_size:
  # morph: gauge
  # Largest component (%)
  current: 0
  target: 0
  unit: "%"

isolated_nodes:
  # morph: number
  0

# ============================================================================
# GRAPH MODELS - Generative mechanisms
# ============================================================================

network_model:
  # morph: badge
  # erdos_renyi | barabasi_albert | watts_strogatz | configuration | stochastic_block | real
  status: ""
  variant: ""

growth_mechanism:
  # morph: badge
  # preferential_attachment | random | fitness | not_applicable
  status: ""
  variant: ""

# ============================================================================
# RESILIENCE & ROBUSTNESS
# ============================================================================

robustness:
  # morph: badge
  # high | moderate | low
  status: ""
  variant: ""

attack_tolerance:
  # morph: badge
  # random_robust | targeted_vulnerable
  status: ""
  variant: ""

percolation_threshold:
  # morph: number
  # Critical fc
  0

# ============================================================================
# BIPARTITE STRUCTURE
# ============================================================================

bipartite:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

bipartite_sets:
  # morph: text
  # e.g., "plants and pollinators"
  ""

set1_size:
  # morph: number
  0

set2_size:
  # morph: number
  0

nestedness:
  # morph: number
  # NODF (0-100)
  0

# ============================================================================
# DIRECTED NETWORK PROPERTIES
# ============================================================================

directed:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

reciprocity:
  # morph: number
  # Fraction of bidirectional edges (0-1)
  0

in_degree_distribution:
  # morph: text
  ""

out_degree_distribution:
  # morph: text
  ""

strongly_connected_components:
  # morph: number
  0

bow_tie_structure:
  # morph: badge
  # yes | no
  # IN, SCC, OUT, tendrils, tubes
  status: ""
  variant: ""

# ============================================================================
# WEIGHTED NETWORK PROPERTIES
# ============================================================================

weighted:
  # morph: badge
  # yes | no
  status: ""
  variant: ""

weight_distribution:
  # morph: badge
  # uniform | normal | power_law | heavy_tailed
  status: ""
  variant: ""

total_weight:
  # morph: number
  0

strength_distribution:
  # morph: text
  # Node strength = sum of edge weights
  ""

# ============================================================================
# SPECTRAL PROPERTIES
# ============================================================================

adjacency_matrix:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

laplacian_matrix:
  # morph: badge
  # computed | not_computed
  status: ""
  variant: ""

largest_eigenvalue:
  # morph: number
  # λ1
  0

spectral_gap:
  # morph: number
  # λ1 - λ2
  0

# ============================================================================
# METADATA
# ============================================================================

data_source:
  # morph: badge
  status: ""
  variant: ""

last_updated:
  # morph: text
  ""

curator:
  # morph: text
  ""

notes:
  # morph: text
  ""

references:
  # morph: list
  items:
    - citation: ""
      doi: ""
      year: ""
