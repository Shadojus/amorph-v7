# BIOFILM NETWORKS - Quorum Sensing & Spatial Organization
# Version: 1.0.0 | Domain: BACTERIA (Holistic)

perspective: biofilm_networks
version: "1.0.0"

scientific_name:
  # morph: text
  ""

biofilm_formation:
  # morph: boolean
  false

# QUORUM SENSING (2025)
quorum_sensing:
  # morph: boolean
  false

qs_systems:
  # morph: list
  # LuxI_LuxR | LasI_LasR | AHL | AI-2
  - ""

autoinducer_types:
  # morph: list
  - ""

qs_regulated_genes:
  # morph: number
  0

qs_threshold_density:
  # morph: number
  # cells/ml
  0

# BIOFILM STRUCTURE
biofilm_architecture:
  # morph: badge
  # mushroom | flat | streamer | tower
  status: ""
  variant: ""

eps_matrix_production:
  # morph: boolean
  # extracellular polymeric substances
  false

eps_composition:
  # morph: pie
  - label: "Polysaccharides"
    value: 0
  - label: "Proteins"
    value: 0
  - label: "DNA"
    value: 0
  - label: "Other"
    value: 0

biofilm_thickness:
  # morph: range
  min: 0
  max: 0
  unit: "μm"

cell_density_in_biofilm:
  # morph: gauge
  value: 0
  min: 0
  max: 10000000000
  unit: "cells/cm²"

# SPATIAL ORGANIZATION
microcolony_formation:
  # morph: boolean
  false

water_channels:
  # morph: boolean
  # nutrient flow through biofilm
  false

metabolic_zonation:
  # morph: boolean
  # aerobic surface, anaerobic depth
  false

# QS INHIBITORS (2025 research)
qs_inhibitor_susceptibility:
  # morph: rating
  rating: 0
  max: 10

natural_qsis:
  # morph: list
  # compounds inhibiting QS
  - ""

# ANTIBIOTIC RESISTANCE
biofilm_antibiotic_resistance:
  # morph: gauge
  # fold increase vs planktonic
  value: 0
  min: 1
  max: 1000
  unit: "fold"

persister_cells:
  # morph: boolean
  false

# DISPERSAL
biofilm_dispersal:
  # morph: boolean
  false

dispersal_triggers:
  # morph: list
  # nutrient_depletion | QS_signal | NO
  - ""

# DATA
data_source:
  # morph: badge
  status: ""
  variant: ""

last_updated:
  # morph: text
  ""
