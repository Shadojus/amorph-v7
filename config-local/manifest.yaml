name: Funginomi
beschreibung: Pilz-Wissenssammlung
version: 1.0.0
sprache: de
autor: AMORPH Team

# === BRANDING ===
# App-Branding (Zeile 0 im Header)
branding:
  titel: FUNGINOMI                    # App-Name/Logo (links)
  titelUrl: /                         # Link bei Klick auf Titel
  
  partner:                            # Partner/Netzwerk (rechts)
    text: Part of the                 # Text vor Partner-Name
    name: Bifroest                    # Partner-Name
    url: https://bifroest.io          # Partner-Link
    
# === FARBEN (App-Level) ===
# Standard-Palette wenn nicht spezifisch definiert
farben:
  palette: standard                   # Welche Palette aus morphs.yaml nutzen
